Setup:
	Copy both these file to <path to>/BigInteger/../
		Command should be something like `cp BigInteger/SampleDriver/* .`
	run `make SampleObjectDirectory`

Compile:
	`make` with your `-j` of choice
	Executable: `execute.out`

Clean:
	`make SampleClean`
