This is a c++ class made for calculations of arbitrarily large numbers

Makefile setup
- Include BigInteger's makefile with
	include BigInteger/BigInteger.mk
- Add as a prerequisite for your executable target
	$(BIGINTEGER_MAIN_OBJ)
- Add as a prerequisite for your clean target
	BigIntegerClean

Before compiling, the objectfile directory needs to be created with
	make BigIntegerObjectDirectory
